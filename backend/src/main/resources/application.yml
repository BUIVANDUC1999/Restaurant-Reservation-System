spring:
  application:
    name: restaurant-reservation
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/restaurant}
    username: ${DB_USERNAME:restaurant}
    password: ${DB_PASSWORD:restaurant}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
  jackson:
    time-zone: Asia/Ho_Chi_Minh
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
server:
  port: ${PORT:8080}
management:
  health:
    mail:
      enabled: ${MAIL_HEALTH_ENABLED:false}
  endpoints:
    web:
      exposure:
        include: health,info
springdoc:
  swagger-ui:
    path: /swagger-ui.html
app:
  demo-tools:
    enabled: ${DEMO_TOOLS_ENABLED:false}
  jwt:
    secret: ${JWT_SECRET:restaurant-reservation-demo-secret-key-change-in-production-2026}
    expiration-minutes: ${JWT_EXPIRATION_MINUTES:480}
  paypal:
    base-url: ${PAYPAL_BASE_URL:https://api-m.sandbox.paypal.com}
    client-id: ${PAYPAL_CLIENT_ID:}
    client-secret: ${PAYPAL_CLIENT_SECRET:}
    currency: ${PAYPAL_CURRENCY:USD}
    vnd-per-usd: ${PAYPAL_VND_PER_USD:25000}
  notifications:
    email-enabled: ${EMAIL_NOTIFICATIONS_ENABLED:false}
    sms-sandbox: ${SMS_SANDBOX:true}
  timeouts:
    reservation-hold-minutes: ${RESERVATION_HOLD_MINUTES:10}
    cleaning-buffer-minutes: ${CLEANING_BUFFER_MINUTES:15}
    upcoming-alert-minutes: ${UPCOMING_ALERT_MINUTES:30}
    late-warning-minutes: ${LATE_WARNING_MINUTES:15}
    late-critical-minutes: ${LATE_CRITICAL_MINUTES:20}
    table-request-ack-minutes: ${TABLE_REQUEST_ACK_MINUTES:3}
    kitchen-critical-overdue-minutes: ${KITCHEN_CRITICAL_OVERDUE_MINUTES:10}
    cleaning-target-minutes: ${CLEANING_TARGET_MINUTES:15}
    monitor-delay-ms: ${TIMEOUT_MONITOR_DELAY_MS:60000}
  walk-in:
    default-wait-minutes: ${WALK_IN_DEFAULT_WAIT_MINUTES:30}
    dining-minutes: ${WALK_IN_DINING_MINUTES:120}
    wait-critical-grace-minutes: ${WALK_IN_WAIT_CRITICAL_GRACE_MINUTES:5}
    offer-warning-minutes: ${WALK_IN_OFFER_WARNING_MINUTES:3}
    offer-expiry-minutes: ${WALK_IN_OFFER_EXPIRY_MINUTES:5}
    seated-warning-minutes: ${WALK_IN_SEATED_WARNING_MINUTES:7}
    seated-critical-minutes: ${WALK_IN_SEATED_CRITICAL_MINUTES:10}
    payment-warning-minutes: ${WALK_IN_PAYMENT_WARNING_MINUTES:3}
    payment-critical-minutes: ${WALK_IN_PAYMENT_CRITICAL_MINUTES:5}
