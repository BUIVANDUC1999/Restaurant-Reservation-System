import{FormEvent,useState}from'react';import{CheckCircle2}from'lucide-react';import{api}from'../api';import type{Reservation}from'../types'
const initial={customerName:'',phone:'',email:'',reservationDate:'',timeSlot:'DINNER',partySize:2,preferredFloor:'NO_PREFERENCE',note:''}
export default function ReservationPage(){const[form,setForm]=useState(initial);const[result,setResult]=useState<Reservation|null>(null);const[error,setError]=useState('');const[loading,setLoading]=useState(false);const change=(k:string,v:string|number)=>setForm({...form,[k]:v});async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{setResult(await api.createReservation(form))}catch(err){setError(err instanceof Error?err.message:'Không thể đặt bàn')}finally{setLoading(false)}}if(result)return <section className="success-card"><CheckCircle2/><p className="eyebrow dark">YÊU CẦU ĐÃ ĐƯỢC GỬI</p><h1>Cảm ơn {result.customerName}!</h1><p>Mã đặt bàn của bạn là</p><strong>{result.code}</strong><p>Nhà hàng sẽ liên hệ xác nhận. Hãy lưu mã này cùng số điện thoại để tra cứu.</p></section>;return <section className="reservation-layout"><div className="reservation-story"><div><p className="eyebrow">ĐẶT BÀN TRỰC TUYẾN</p><h1>Sẵn sàng cho một<br/>bữa ăn đáng nhớ?</h1><p>Chọn ngày, ca phục vụ và số khách. Yêu cầu mới sẽ được nhà hàng kiểm tra và xác nhận.</p><ul><li>Ca trưa bắt đầu từ 11:00</li><li>Ca tối bắt đầu từ 17:30</li><li>Đoàn trên 50 khách được tư vấn riêng</li></ul></div></div><form className="reservation-form" onSubmit={submit}><p className="eyebrow dark">THÔNG TIN ĐẶT BÀN</p><h2>Giữ chỗ của bạn</h2><div className="form-grid"><label>Họ và tên<input required value={form.customerName} onChange={e=>change('customerName',e.target.value)} placeholder="Nguyễn Văn An"/></label><label>Số điện thoại<input required value={form.phone} onChange={e=>change('phone',e.target.value)} placeholder="0984 353 577"/></label><label>Email<input type="email" value={form.email} onChange={e=>change('email',e.target.value)} placeholder="ban@example.com"/></label><label>Ngày dùng bữa<input required type="date" min={new Date().toISOString().slice(0,10)} value={form.reservationDate} onChange={e=>change('reservationDate',e.target.value)}/></label><label>Ca phục vụ<select value={form.timeSlot} onChange={e=>change('timeSlot',e.target.value)}><option value="LUNCH">Bữa trưa — 11:00</option><option value="DINNER">Bữa tối — 17:30</option></select></label><label>Số khách<input required type="number" min="1" max="300" value={form.partySize} onChange={e=>change('partySize',Number(e.target.value))}/></label><label>Tầng mong muốn<select value={form.preferredFloor} onChange={e=>change('preferredFloor',e.target.value)}><option value="NO_PREFERENCE">Không yêu cầu</option><option value="FLOOR_1">Tầng 1 — 120 ghế</option><option value="FLOOR_2">Tầng 2 — 180 ghế</option></select></label><label className="wide">Ghi chú<textarea value={form.note} onChange={e=>change('note',e.target.value)} placeholder="Sinh nhật, ăn chay, ghế trẻ em..."/></label></div>{error&&<p className="error">{error}</p>}<button disabled={loading} className="btn btn-green">{loading?'Đang gửi...':'Gửi yêu cầu đặt bàn'}</button></form></section>}

