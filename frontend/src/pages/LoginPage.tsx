import{FormEvent,useState}from'react';import{LockKeyhole}from'lucide-react';import{useLocation,useNavigate}from'react-router-dom';import{api}from'../api';import{useAuth}from'../auth'
export default function LoginPage(){const[email,setEmail]=useState('admin@khamphaviet.vn');const[password,setPassword]=useState('Admin@123');const[error,setError]=useState('');const[loading,setLoading]=useState(false);const{save}=useAuth();const navigate=useNavigate();const location=useLocation();async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');try{save(await api.login(email,password));navigate((location.state as {from?:string})?.from||'/staff',{replace:true})}catch(err){setError(err instanceof Error?err.message:'Đăng nhập thất bại')}finally{setLoading(false)}}return <section className="login-page"><form className="login-card" onSubmit={submit}><div className="login-icon"><LockKeyhole/></div><p className="eyebrow dark">KHU VỰC NHÂN VIÊN</p><h1>Đăng nhập quản trị</h1><p>Dùng tài khoản được nhà hàng cấp để quản lý đặt bàn và sơ đồ bàn.</p><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Mật khẩu<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<p className="error">{error}</p>}<button className="btn btn-green" disabled={loading}>{loading?'Đang đăng nhập...':'Đăng nhập'}</button><small>Tài khoản demo: admin@khamphaviet.vn / Admin@123</small></form></section>}

