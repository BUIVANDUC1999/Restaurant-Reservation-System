import{useEffect,useMemo,useState}from'react';import{api}from'../api';import type{MenuItem}from'../types';import MenuGrid from'../components/MenuGrid'
export default function MenuPage(){const[items,setItems]=useState<MenuItem[]>([]);const[cat,setCat]=useState('Tất cả');useEffect(()=>{api.menu().then(setItems)},[]);const cats=['Tất cả',...new Set(items.map(x=>x.category))];const shown=useMemo(()=>cat==='Tất cả'?items:items.filter(x=>x.category===cat),[items,cat]);return <section className="page-section container"><p className="eyebrow dark">HƯƠNG VỊ SA PA</p><h1>Thực đơn nhà hàng</h1><p className="page-lead">Từ thắng cố truyền thống, cá hồi Sa Pa đến lẩu, đồ nướng và thực đơn đoàn.</p><div className="filters">{cats.map(c=><button className={cat===c?'active':''} onClick={()=>setCat(c)} key={c}>{c}</button>)}</div><MenuGrid items={shown}/></section>}

