import{FormEvent,useState}from'react';import{ShoppingBasket}from'lucide-react';import{api}from'../api';import type{Reservation}from'../types'
export default function LookupPage(){const[code,setCode]=useState('');const[phone,setPhone]=useState('');const[data,setData]=useState<Reservation|null>(null);const[error,setError]=useState('');async function submit(e:FormEvent){e.preventDefault();setError('');try{setData(await api.lookup(code,phone))}catch(err){setData(null);setError(err instanceof Error?err.message:'Không tìm thấy')}}return <section className="lookup page-section container"><p className="eyebrow dark">TRA CỨU NHANH</p><h1>Đơn đặt bàn của bạn</h1><form onSubmit={submit}><input required value={code} onChange={e=>setCode(e.target.value)} placeholder="Mã đặt bàn, ví dụ KV-ABC123"/><input required value={phone} onChange={e=>setPhone(e.target.value)} placeholder="Số điện thoại"/><button className="btn btn-green">Tra cứu</button></form>{error&&<p className="error">{error}</p>}{data&&<div className="lookup-result"><span className={`status ${data.status.toLowerCase()}`}>{data.status}</span><h2>{data.customerName}</h2><dl><div><dt>Mã đơn</dt><dd>{data.code}</dd></div><div><dt>Ngày</dt><dd>{data.reservationDate}</dd></div><div><dt>Ca</dt><dd>{data.timeSlot==='LUNCH'?'Bữa trưa':'Bữa tối'}</dd></div><div><dt>Số khách</dt><dd>{data.partySize}</dd></div></dl>{data.preOrderItems?.length>0&&<div className="lookup-preorder"><h3><ShoppingBasket/> Món đã chọn trước</h3>{data.preOrderItems.map(item=><div key={item.id}><span>{item.quantity}× {item.itemName}</span><b>{Number(item.lineTotal).toLocaleString('vi-VN')}đ</b><i className={item.status.toLowerCase()}>{item.status==='CONFIRMED'?'Đã xác nhận':'Chờ nhân viên xác nhận'}</i></div>)}<strong>Tạm tính: {data.preOrderItems.reduce((sum,item)=>sum+Number(item.lineTotal),0).toLocaleString('vi-VN')}đ</strong></div>}</div>}</section>}
